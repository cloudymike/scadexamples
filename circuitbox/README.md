# Box generator from board layout files

The goal of this design is to create a box for a circuit board
based on the gocode file.

The main python file baseplategen.py creates the openscad commands to create a baseplate with mounts.

You will have to pip install requirements.txt